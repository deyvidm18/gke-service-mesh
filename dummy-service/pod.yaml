apiVersion: v1
kind: Pod
metadata:
  name: simple-pod
  namespace: my-mesh
  labels:
    app: simple-pod
spec:
  containers:
  - name: simple-pod
    image: nginx:alpine
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      memory: 256Mi
    ports:
    - containerPort: 80
